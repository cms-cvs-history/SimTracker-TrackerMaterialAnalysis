# Random number generators for (optional) modules up to trackingMaterialProducer
service = RandomNumberGeneratorService 
{
  PSet theSource = {
    untracked uint32 initialSeed = 442302
    untracked string engineName = "HepJamesRandom"
  }

  PSet VtxSmeared = {
    untracked uint32 initialSeed = 863771
    untracked string engineName = "HepJamesRandom"
  }

  PSet trackingMateriarlProducer = {
    untracked uint32 initialSeed = 288269
    untracked string engineName = "HepJamesRandom"
  }

}
